<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong><%= @user.name%>'s created events</strong>
  <br /> 
  
  <% @user.created_events.each do |e| %>
    <tr>
      <td> Event name: <%= e.name %></td>
      <td><%= link_to 'Show', e %></td>
    </tr><br />
  <% end %>

    
</p>

<p>
  <strong><%= @user.name%>'s attended events</strong>
  <br /> 
  
  <% @user.attended_events.each do |e| %>
    <tr>
      <td> Event name: <%= e.name %></td>
      <td><%= link_to 'Show', e %></td>
    </tr><br />
  <% end %>

    
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
